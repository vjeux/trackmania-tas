2026-08-24T19:59:24Z	tried	what=push retry under a moving remote	config=both push routes retry up to 3 times, re-syncing each round	produced=the bank that had failed went through; origin/main and local HEAD agree	claim=MEASURED	control=a test builds a bare origin and two clones, syncs, THEN lands the other clone's commit, and requires the push to still land and both pieces of work to survive — a single-shot push fails that test	why=The first rebase fix closed the case where the remote had already moved. It could not close the window between our fetch and our push, which is where the second failure landed. Two unrelated sessions pushed to this repo in the harness's first hour, so the window is not theoretical.	node=117796
