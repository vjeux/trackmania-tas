2026-08-24T23:38:12Z	tried	what=banking the validator-car resolver bundle	config=sha256-verified bundle, fetched to refs/remotes/incoming/validator-car, merge attempted in a scratch worktree, then pushed to origin as its OWN branch rather than merged into main	produced=origin/validator-car = 4fb42ad6b228b814679f5718d29d34eb9ab3407e, byte-identical to the sha the lead named. A merge into main conflicts in 119 hunks across 17 files, all under tools/, none under autopilot/	claim=MEASURED	control=sha256 of the bundle computed here matches the one supplied; git bundle verify reports a complete history; and the push driver refuses to report success unless the sha the render box pushed equals the local ref's	why=Two independent lineages: main carries agent C2's and vjeux's tools/tmauto and tools/ghost work, the bundle carries agent C's original lineage plus the validator-car commits, and no cherry-pick equivalence between them. Resolving 119 hunks of somebody else's engine source by picking a side would silently drop landed work — and in this project a wrong resolution produces plausible wrong numbers rather than an error. Banking it as a branch loses nothing, invents nothing, and leaves the adjudication to whoever owns tmauto.	node=42504
2026-08-24T23:38:12Z	tried	what=the harness's crates were dropped from the workspace and its tmmaps additions reverted	config=tools/Cargo.toml members restored to include haul and resim; tmmaps authortime and try_load restored from the original commit by git apply --3way	produced=cargo build -p haul -p resim works again on a clean clone; 131 + 21 + 7 tests pass	claim=MEASURED	control=the failure itself was the control: 'cargo build -p haul' answered 'package ID specification haul did not match any packages', which is the loud form. The quiet half is what mattered — the tmmaps revert compiled fine because nothing that used it was in the workspace any more	why=The members list is not a preference, it is the test surface. A crate outside it is a crate nobody compiles, so a revert of its dependencies is invisible. This also broke the first line of the documented bootstrap that every heartbeat runs on a fresh box.	node=42504
