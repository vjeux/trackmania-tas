2026-08-25T07:41:20Z	tried	what=the credential bootstrap could not reach a box that had never pushed	config=credential serve now unions the repo's box registry with node names parsed out of recent TMHAUL-STATE mirror paste titles; the launcher's lock is PID-aware	produced=24576 was discovered and served within one --once pass; before the change it sat DEGRADED with the server running and finding nothing	claim=MEASURED	control=five parse tests including a stale announcement, a duplicate, the server's own name, and a title of the right prefix but the wrong shape — that last one matters because the parsed token becomes an ssh hostname. And the lock fix was proved from the failure: kill -9, confirm the lock survived, watch the launcher reclaim it	why=A bootstrap must not depend on the capability it is bootstrapping. A fresh box writes its registration into the repo and cannot push it — pushing is what it needs the credential FOR — so the registry the server reads never learned it existed. The mirror paste needs only an x509 cert, which every box has from its first minute, so that is the announcement channel that still works when the one being bootstrapped does not.	node=24576
