2026-08-25T22:02:42Z	tried	what=the supervisor_died alarm	config=samples already carry their writing node; the view gains supervisor_here and this_node, filled by state::with_this_box, and the alarm fires only when the run is active AND this box wrote the last sample AND no supervisor process is alive here	produced=fired CRITICAL against the real dead supervisor on 119098 before I restarted it; 12 alarms now, selftest clean; 146 tests	claim=MEASURED	control=four silence tests: a live supervisor, a box that does not own the run, an unchecked box (None must not read as 'no'), and an idle project. Without the ownership one this alarm would scream on the devserver and on every heartbeat box that merely reads the repo	why=The harness already knew — beat prints NOT RUNNING — and the alarm surface did not. A check that exists but is not wired to what people read is not a check. Also learned: pgrep -f matches the shell command containing the pattern, so my ad-hoc liveness checks could answer yes with nothing running; the alarm's /proc parse was right where my shell was wrong.	node=119098
