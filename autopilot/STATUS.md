# TM2020 autopilot — status

*Generated by `tmhaul status` at 2026-08-25T11:30:10Z. Every number below is read out of
`autopilot/state/` at generation time; nothing here is typed in by hand.*

## Right now

**Running.** 0.3 evals/s over the last 10m.

| | |
|---|---|
| map | **Summer 2026 - 01** |
| rung | 1 — reach CP1 (the gate at 512 m, station 25 of 97); author time 23.144 |
| objective | tapes confirmed by the plain oracle this pass |
| best objective ever seen | 112 |
| run started | 2026-08-25T07:36:03Z (3h 54m ago) |
| GitHub banking | working |
| last banked | 16m ago — commit 05b37cd309b4 · mirror P2475158480 · push whitestick→github 05b37cd309b4 (bundle md5 8d5383b9) |

## Alarms

None firing.

That sentence is only worth reading because every alarm has a test that
makes it fire: `cd tools/haul && cargo test`, or `tmhaul alarms selftest` to
watch each one fire on this box right now.

## Budget — work done, not time passed

The pre-committed switch condition is **8,000,000 evals or 10h 0m of productive time**,
after which a learned ordering over archive bins gets added (DESIGN.md §3.2).

| | |
|---|---|
| evals | 4,795 |
| productive time | 2h 28m of 10h 0m |
| stalled time (does **not** spend the budget) | 2h 36m |
| spent | 24.7% |
| switch reached | no |

## Queue

0 pending · 0 claimed (0 expired) · 0 completed

## Boxes

| box | state | lease | last seen |
|---|---|---|---|
| `117796` | retired | 2026-08-25T06:18:00Z (expired) | 14h 57m ago |
| `24576` | **active** | 2026-08-25T19:34:00Z (8h 3m left) | 12s ago |
| `42504` | retired | 2026-08-25T08:30:00Z (expired) | 3h 58m ago |

## What has been tried

Newest first. The `why` is the part that will still be worth reading later.

**2026-08-25T07:41:20Z** — the credential bootstrap could not reach a box that had never pushed · *MEASURED*

- config: `credential serve now unions the repo's box registry with node names parsed out of recent TMHAUL-STATE mirror paste titles; the launcher's lock is PID-aware`
- produced: 24576 was discovered and served within one --once pass; before the change it sat DEGRADED with the server running and finding nothing
- why: A bootstrap must not depend on the capability it is bootstrapping. A fresh box writes its registration into the repo and cannot push it — pushing is what it needs the credential FOR — so the registry the server reads never learned it existed. The mirror paste needs only an x509 cert, which every box has from its first minute, so that is the announcement channel that still works when the one being bootstrapped does not.
- control: five parse tests including a stale announcement, a duplicate, the server's own name, and a title of the right prefix but the wrong shape — that last one matters because the parsed token becomes an ssh hostname. And the lock fix was proved from the failure: kill -9, confirm the lock survived, watch the launcher reclaim it

**2026-08-25T06:02:34Z** — the sweep was spending the archive search's pre-committed budget · *MEASURED*

- config: `budget intervals now carry a budget_key from the job; totals are per budget; legacy rows read as 'unattributed' rather than being absorbed; job.rec sets budget_key = resim-sweep`
- produced: the archive search's budget reads 0 evals / 0s, which is true — it has never run under this harness. The 9705 evals and 5h 5m of productive time are the sweep's, and tmhaul budget now says so on a separate line
- why: The harness was built so a stall could not spend this budget, and then it spent five hours of it on the wrong workload — I caught it at 50.8%, about five hours before it would have announced 'the switch is due' for a job the condition was never about. 'Counts work, not time' is not enough; it has to count THE work the threshold was agreed about. The same sentence I wrote about stalls applies one level up, and I did not see it until the number was half spent.
- control: three tests: two budgets in one directory do not contaminate each other while the cross-budget total still sums both; a keyless legacy interval lands in 'unattributed' and NOT in the search's; and ten productive hours of the wrong workload does not trip the switch for the right one

**2026-08-25T00:45:35Z** — pushing the Sorry LOL tmsite acquisition commit · *MEASURED*

- config: `bundle fetched from the bank, single commit rebased onto current main in a scratch worktree, built and tested there, fast-forwarded, banked through the normal workflow`
- produced: origin/main = f586ad26f81067c78496131094c827349bc7538c, carrying 9011c96 'Add single-map acquisition to tmsite' (3 files, +190). No conflicts: it touches only tools/tmsite
- why: Scoped narrowly on purpose: the request was for the optimizer commit, and this commit touches three files in one crate. That is why it needed no adjudication, unlike the validator-car bundle sitting on its own branch.
- control: the bank's own MANIFEST.md5 verified 270 of 270 entries on the writing box before anything was read; the rebased tree was built and tested before the fast-forward; and five concurrent commits from other sessions were each confirmed still ancestors of the pushed head

**2026-08-24T23:38:12Z** — the harness's crates were dropped from the workspace and its tmmaps additions reverted · *MEASURED*

- config: `tools/Cargo.toml members restored to include haul and resim; tmmaps authortime and try_load restored from the original commit by git apply --3way`
- produced: cargo build -p haul -p resim works again on a clean clone; 131 + 21 + 7 tests pass
- why: The members list is not a preference, it is the test surface. A crate outside it is a crate nobody compiles, so a revert of its dependencies is invisible. This also broke the first line of the documented bootstrap that every heartbeat runs on a fresh box.
- control: the failure itself was the control: 'cargo build -p haul' answered 'package ID specification haul did not match any packages', which is the loud form. The quiet half is what mattered — the tmmaps revert compiled fine because nothing that used it was in the workspace any more

**2026-08-24T23:38:12Z** — banking the validator-car resolver bundle · *MEASURED*

- config: `sha256-verified bundle, fetched to refs/remotes/incoming/validator-car, merge attempted in a scratch worktree, then pushed to origin as its OWN branch rather than merged into main`
- produced: origin/validator-car = 4fb42ad6b228b814679f5718d29d34eb9ab3407e, byte-identical to the sha the lead named. A merge into main conflicts in 119 hunks across 17 files, all under tools/, none under autopilot/
- why: Two independent lineages: main carries agent C2's and vjeux's tools/tmauto and tools/ghost work, the bundle carries agent C's original lineage plus the validator-car commits, and no cherry-pick equivalence between them. Resolving 119 hunks of somebody else's engine source by picking a side would silently drop landed work — and in this project a wrong resolution produces plausible wrong numbers rather than an error. Banking it as a branch loses nothing, invents nothing, and leaves the adjudication to whoever owns tmauto.
- control: sha256 of the bundle computed here matches the one supplied; git bundle verify reports a complete history; and the push driver refuses to report success unless the sha the render box pushed equals the local ref's

**2026-08-24T20:57:11Z** — the bridge credential bootstraps itself · *MEASURED*

- config: `tmhaul credential check|serve|selftest; serve runs on the devserver under cron (@reboot plus every 15 minutes, one instance by lock dir), reads the box registry from the repo, and pushes to any live box that lacks the file`
- produced: deleted the credential from the running box twice: the first time the devserver's --once pass installed it (mode 600, 160 bytes, owner vjeux); the second time nobody intervened at all and the 5-minute cycle healed it within one period. In between, the status page said DEGRADED and banking_degraded fired
- why: An otherwise autonomous rotation with one manual file copy is not autonomous, and the goal is months unattended. The interesting part is the direction: an OD cannot reach a devserver, so this could not be a pull by the box that needs the file — the machine that HAS the credential pushes it, aimed by a registry the repo already publishes. Hostnames are not secrets; the credential never touches the repo, a log, a paste or an error body.
- control: tmhaul credential selftest is the arm that matters: a delivery to an unreachable host must REFUSE, its reason must be a category rather than a transport body, and an absent file must read Absent rather than ok. Plus the positive control of this box's own working credential, so the three refusals are not passing vacuously on a box where everything fails

**2026-08-24T20:36:50Z** — a false disk alarm caused by the rotation itself · *MEASURED*

- config: `samples now carry their writing node; the disk trend compares within one box only, and needs a 30-minute window before it will extrapolate`
- produced: the alarm cleared; 119 tests pass; selftest still fires all 10 alarms
- why: The alarm was right that free space had dropped by 850 GB and wrong that anything was filling: it compared two different machines. Worth recording because the general shape bites any long-haul metric — most of them are about the RUN and survive a box change, and the few that are about the MACHINE do not. A false critical on a routine event is how an alarm gets ignored, so it earned a fix and not a threshold.
- control: two suppression tests (a rotation, a bootstrap) plus a test requiring a genuine one-box slope AND the free-space cliff to still fire — suppressions without that third test would be indistinguishable from switching the alarm off

**2026-08-24T20:34:25Z** — the first unplanned box rotation, end to end · *MEASURED*

- config: `117796's lease reclaimed at 20:30Z with no stand-down; fresh box 42504 provisioned, bootstrapped from the repo, recovered, dead box retired, supervisor restarted`
- produced: clone+build 5.2s; recover found 47 files identical between the GitHub clone and the paste mirror; the worker resumed at resume_evals=1068 read out of the repo; the budget did NOT re-count the resume point; the new supervisor banked and pushed 6a38e0f0 on its own with no proxy in its environment; all alarms clear
- why: This is the rotation the whole harness exists for, and it happened for real rather than as a drill: the lease went away underneath a running supervisor. Everything held except two things worth having found — a vanished box could not be retired at all, and the bridge credential has to be re-copied to each new box.
- control: tmhaul beat, run on the fresh box before any intervention, said exactly 'the run belongs to a box that is gone; take it over here' and exited 3 — the diagnosis came from committed state, since that box had never seen this project

**2026-08-24T20:05:00Z** — a silent fetch failure that made the retry loop lie · *MEASURED*

- config: `sync_with_remote returned Ok(None) on a failed git fetch; git network calls now go through gitcmd::git_env, which supplies the forward proxy unless the environment already sets one; rebase uses --autostash`
- produced: a full bank under 'env -u https_proxy -u http_proxy' now commits, mirrors and pushes; origin/main == local HEAD at 3e3f22b3, with both other sessions' commits (992c438, 5fa72b8) ancestors of ours
- why: This is the project's signature bug INSIDE the harness written to catch it, and my retry loop turned a silent no-op into a confident false explanation — worse than the original failure, because it named a cause. Two lessons: a fetch that could not run is UNKNOWN and never 'nothing to do'; and a detached process inherits nothing, so any dependency on an exported variable works interactively and fails unattended.
- control: the negative arm is the run itself: stripping every proxy variable from the environment is the condition that used to fail silently, and it now succeeds. Before the fix the same command reported 'the remote kept moving' three times against a remote that had not moved

**2026-08-24T19:59:24Z** — push retry under a moving remote · *MEASURED*

- config: `both push routes retry up to 3 times, re-syncing each round`
- produced: the bank that had failed went through; origin/main and local HEAD agree
- why: The first rebase fix closed the case where the remote had already moved. It could not close the window between our fetch and our push, which is where the second failure landed. Two unrelated sessions pushed to this repo in the harness's first hour, so the window is not theoretical.
- control: a test builds a bare origin and two clones, syncs, THEN lands the other clone's commit, and requires the push to still land and both pieces of work to survive — a single-shot push fails that test

**2026-08-24T19:57:12Z** — correcting an independence claim I made about the start line · *SUPERSEDED*

- config: `the start-position check compares against the map's DECLARED Spawn waypoint`
- produced: the code comment, HARNESS.md and this ledger now say ONE source
- why: I wrote that the map's Spawn cell and the lead's 3D render independently agreed on (1584, 784). They do not: the render showed the route mesh and the suspect trajectory and never identified the start line. Two readings that share a source prove nothing, and I asserted corroboration that did not exist while writing a check whose entire purpose is catching that class of error. An independent second source means reading tick 0 out of a live re-simulation, which is what G2 now requires of a driving worker.
- control: none — this is a retraction, and it is recorded because the corrected version is weaker than what I reported

**2026-08-24T19:57:12Z** — the four acceptance gates · *MEASURED*

- config: `tmhaul claim, the only route from a worker's output to banked state: G1 reproducible (frame + prefix + map/template/tape hashes), G2 live tick 0 for a driving worker, G3 fail closed with no start control, G4 the oracle's raw transcript banked beside the claim`
- produced: a claim of the shape the explorer currently produces is refused by all four; a complete one is accepted and banks every hash, the frame, the prefix or the word root, tick 0 and the transcript's md5
- why: The explorer's five confirmed tapes could not be reproduced: wrong start position or a state locator on the wrong object, and no replay frame. The lead held integration back, correctly. What generalises is not a fix to that explorer but the standard every worker must meet — a tape nobody can re-derive is not a result, and an unchecked start is not a passing one.
- control: tmhaul gates walks seven broken claims and one good one — each breaks exactly ONE field of the same complete claim, so a test can only pass by exercising the gate it names, and the good claim must be accepted. A test asserts --no-drive exempts G2 and nothing else, so the flag cannot become a way to switch the gates off

**2026-08-24T19:48:07Z** — an absent row is not a passing row, again · *MEASURED*

- config: `the sweep's results log recorded only tapes that reached the oracle`
- produced: 114 rows for 174 tapes; the 60 refused and unplaceable ones left no trace, so a log covering three quarters of the corpus read as a clean sweep of all of it
- why: Fourth instance of this bug class in this project and the first inside the harness I built to catch it. Writing the row for the case you did not measure is the whole discipline, and I still had to be shown it by counting lines.
- control: the count itself: 174 rows for 174 tapes after the fix, checked by wc

**2026-08-24T19:48:07Z** — the first full standing re-simulation sweep · *MEASURED*

- config: `tmresim over all 36 map dirs, plain oracle in process, one server launch per map, against a 94-row map registry`
- produced: 174 tapes in 3m: 112 hold, 0 differ, 1 DNF (a file named DO_NOT_PUBLISH, declaring 40.226, doing DNF cp5), 1 STARTED-ELSEWHERE (magnet trial's CUT tape, 1089.2 m out), 13 refused as human, 47 UNMEASURED
- why: The rule that every banked result is re-simulated STANDING has been in RULES.md all along with nothing performing it. The valuable output is not the 112: it is the 47. Better than a quarter of the corpus cannot be placed by provenance, so it is not covered — and the fix is the PROV record, not a wider list of filename prefixes, because guessing that best_* is probably ours is exactly the reasoning that leaks a ghost.
- control: 0 differ across 112 tapes is the claim; the DNF row is the control that the detector can say no, since that file was already known to declare a time it does not achieve. The start check is separately two-sided: 10.6 m on a real tape at a 0.001 m tolerance, silent at 32 m

**2026-08-24T19:19:37Z** — two harness bugs found by running it, not by reading it · *MEASURED*

- config: `supervisor eval baseline seeded from the repo; watch refuses to start beside a live supervisor; budget gains an append-only correction record`
- produced: the budget read 582 evals for 308 done and now reads 308; a second supervisor is refused by name and pid instead of silently coexisting
- why: Both bugs are invisible to a reading and obvious within twenty minutes of running. The budget one is the more serious: it inflates in the direction of switching to a learned ordering too early, and it does so precisely because restart-and-resume is the system's NORMAL mode rather than an exception. Worth remembering that the harness's own instruments need the same suspicion as the project's.
- control: the seeding test asserts 194 from a repo that says 194 AND zero from an empty repo — a seeder that always returns the last number it saw would pass only the first half; the correction test asserts the log still has three lines, so nothing was rewritten

## Recent events

```
2026-08-25T07:06:00Z	bank	why=periodic	receipt=commit 0076f88efc9d · mirror P2474941969 · push whitestick→github 0076f88efc9d (bundle md5 ef331e12)
2026-08-25T07:31:00Z	run_stop	why=stop file	node=42504
2026-08-25T07:31:58Z	bank	why=stand down	receipt=commit fdaecb3b9ce2 · mirror P2474964376 · push whitestick→github fdaecb3b9ce2 (bundle md5 581786b5)
2026-08-25T07:32:00Z	verified	result=every banked file matches its md5 in the commit
2026-08-25T07:36:03Z	worker_start	pid=220670	cmd=TM_SERVER=/tmp/tmoracle/server /tmp/tmtas/tools/target/release/tmresim --repo /tmp/tmtas --results /tmp/tmtas/autopilot/state/frontier/resim-results.rec --loop --pause-s 300	resume_evals=10788	resume_best=112
2026-08-25T07:36:03Z	run_start	node=24576	binary_built=2026-08-25T07:35:17Z	cmd=TM_SERVER=/tmp/tmoracle/server /tmp/tmtas/tools/target/release/tmresim --repo /tmp/tmtas --results /tmp/tmtas/autopilot/state/frontier/resim-results.rec --loop --pause-s 300	map=Summer 2026 - 01	rung=1 — reach CP1 (the gate at 512 m, station 25 of 97); author time 23.144
2026-08-25T07:36:09Z	bank	why=run start	receipt=commit 753114f3ed9a · mirror P2474969284 · push off
2026-08-25T07:37:09Z	alarm	id=banking_degraded	detail=the bridge credential is missing or the bridge does not answer, so pushes to GitHub are failing. The paste mirror still works, so no work is at risk — but the repo a human reads is going stale. `tmhaul credential check`; the devserver's `tmhaul credential serve` should heal it
2026-08-25T07:39:09Z	bank	why=mirror-based credential discovery	receipt=commit 9a9a6996474f · mirror P2474972015 · push off
2026-08-25T07:40:09Z	alarm_clear	id=banking_degraded
2026-08-25T07:42:08Z	bank	why=credential discovery and the launcher	receipt=commit b7c9c4e2c4b6 · mirror P2474973953 · push whitestick→github b7c9c4e2c4b6 (bundle md5 bd3a1684)
2026-08-25T08:01:10Z	bank	why=heartbeat	receipt=commit 17f71f68c6b9 · mirror P2474990151 · push whitestick→github 17f71f68c6b9 (bundle md5 34f35056)
2026-08-25T08:07:01Z	bank	why=periodic	receipt=commit ed717492a89b · mirror P2474996021 · push whitestick→github ed717492a89b (bundle md5 362585ff)
2026-08-25T08:31:07Z	bank	why=heartbeat	receipt=commit 67918da4bfb5 · mirror P2475016503 · push whitestick→github 67918da4bfb5 (bundle md5 dd817197)
2026-08-25T08:37:58Z	bank	why=periodic	receipt=commit 4b6973415ec2 · mirror P2475023012 · push whitestick→github 4b6973415ec2 (bundle md5 ffb8a0b8)
2026-08-25T09:01:10Z	bank	why=heartbeat	receipt=commit 113ade0814c9 · mirror P2475041650 · push whitestick→github 113ade0814c9 (bundle md5 d3f05e4a)
2026-08-25T09:08:56Z	bank	why=periodic	receipt=commit 00bfc86e4dea · mirror P2475048754 · push whitestick→github 00bfc86e4dea (bundle md5 e221df05)
2026-08-25T09:31:08Z	bank	why=heartbeat	receipt=commit 4d7f876a27ab · mirror P2475068077 · push whitestick→github 4d7f876a27ab (bundle md5 c09d4b30)
2026-08-25T09:39:44Z	bank	why=periodic	receipt=commit f5c161ae1ed1 · mirror P2475075512 · push whitestick→github f5c161ae1ed1 (bundle md5 3928f284)
2026-08-25T10:01:05Z	bank	why=heartbeat	receipt=commit c8f4ad6ca287 · mirror P2475095162 · push whitestick→github c8f4ad6ca287 (bundle md5 9e4236a0)
2026-08-25T10:12:03Z	bank	why=periodic	receipt=commit 6a41be0ba7dc · mirror P2475103251 · push whitestick→github 6a41be0ba7dc (bundle md5 f6f384e6)
2026-08-25T10:31:03Z	bank	why=heartbeat	receipt=commit 22ed5182d9c5 · mirror P2475119040 · push whitestick→github 22ed5182d9c5 (bundle md5 64089000)
2026-08-25T10:42:54Z	bank	why=periodic	receipt=commit 4e8de2db7028 · mirror P2475127954 · push whitestick→github 4e8de2db7028 (bundle md5 327c6328)
2026-08-25T11:01:07Z	bank	why=heartbeat	receipt=commit af673104c785 · mirror P2475145816 · push whitestick→github af673104c785 (bundle md5 03a2e6d4)
2026-08-25T11:13:58Z	bank	why=periodic	receipt=commit 05b37cd309b4 · mirror P2475158480 · push whitestick→github 05b37cd309b4 (bundle md5 8d5383b9)
```

---

*Operating this thing: `autopilot/HARNESS.md`. Failures we have already paid
for and their fixes: `autopilot/OPS-LOG.md`.*
