[package]
name = "circuit"
version.workspace = true
edition.workspace = true
description = "Real-world circuit -> TM2020 map: OSM outline + LIDAR terrain -> custom items -> .Map.Gbx"

[dependencies]
tmmaps = { path = "../tmmaps" }
mapgeom = { path = "../mapgeom" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
miniz_oxide = "0.9.1"
