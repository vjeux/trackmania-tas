[package]
name = "shootctl"
version.workspace = true
edition = "2021"

[[bin]]
name = "shootctl"
path = "src/main.rs"

# No [profile]: this is a workspace member, so cargo IGNORES a profile here and
# says so on every build. The workspace root sets opt-level 2, which is what
# this crate asked for anyway.
